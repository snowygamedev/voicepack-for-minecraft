# Normalise to LF in the repository so the Linux and macOS release builds see
# the same bytes as a Windows checkout.
* text=auto eol=lf

# Batch files must keep CRLF or cmd.exe mis-parses them.
*.cmd text eol=crlf

*.png binary
*.ico binary
*.icns binary
